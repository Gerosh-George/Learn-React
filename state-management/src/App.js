//import MovieApp from "./Context API"
import MovieApp from "./Redux";

function App() {
	return (
		<div>
			{
				// Movie app to try out context API
				//toggle the import to see the difference
			}

			{
				// Movie app to try out redux basics
				/*
          				Redux is a state management tool which can used with any framework
         				 STORE ACTION REDUCER DISPATCHER - Four components for redux to work
        		*/
			}

			<MovieApp />
		</div>
	);
}

export default App;
